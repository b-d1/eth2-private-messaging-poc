{
  "name": "eth2-validator-private-messaging",
  "version": "1.0.0",
  "description": "ETH2 Validator private messaging",
  "main": "dist/src/index.js",
  "author": "Blagoj Dimovski",
  "license": "MIT",
  "dependencies": {
    "js-waku": "^0.14.1",
    "protons": "^2.0.3"
  },
  "devDependencies": {
    "@types/node": "^16.9.4",
    "prettier": "^2.4.1",
    "tslint": "^6.1.3",
    "typescript": "^4.4.3"
  },
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "prettier --write \"**/*.{js,json,md,sol,ts}\""
  }
}
